Function: It is a block of code that is realted to particular functionality 
Different types of functin:
1) Function Declaration 
2) Anonymous function(function without any name)
3) CallBack Function 
4) Higher order Function 
5) Arrow Function 
6) Calculator Function 
7) 
var helloFun = function(){
    console.log("Hello")
}
helloFun()

There are some inbuilt functions:
1) for array
2) for string 
3) for maths


object: It is a real world entiity wgich using state and behavious where state tells how they look and behavoir 

Object: it is collections of key vakue pairs it can store hetrogenous type of data 
//where key value pair - one entry, in an object we can have multiple entries 
//declartion 
 var students = {}// object literal 
 //console.log(student)
//initializatiob(storing data) of object- there are two ways to add data to the object 
1) using dot notation(.) //  student.name="Rakesh"

2) using square bracket notation ([])

 //higher order function: 
 A Higher-Order Function is a function that:
Takes another function as an argument, OR
Returns another function.
Interview Definition:
A Higher-Order Function is a function that accepts one or more functions as arguments or returns a function as its result.
//code
function greet(name) {
    return "Hello " + name;
}

function processUser(func) {
    console.log(func("Komal"));// this becomes console.log(greet("Komal"));
}

processUser(greet);// hello komal 
